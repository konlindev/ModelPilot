# Create modelpilot-gateway skeleton

- 时间：20260605-164501
- 分支：main-update-20260605-164501
- 基准分支：main

## 主要变更

- 新增 `modelpilot-gateway` Python 项目骨架。
- 配置 FastAPI 基础应用和 `GET /health`。
- 添加 pytest 健康检查测试。
- 添加项目 README、requirements、示例配置和日志目录占位。
- 修正更新分支命名格式为 `main-update-yyyyMMdd-HHmmss`，避免 Git ref 命名冲突。

## 验证方式

已执行：

```powershell
python -m pytest
```

结果：`1 passed, 1 warning`。

已执行：

```powershell
python -c "import app; import app.main; import app.config_loader; import app.schemas; import app.auth; import app.router_engine; import app.classifier; import app.validators; import app.backend_clients; import app.quota; import app.logging_config; import app.utils; print(app.__version__)"
```

结果：输出 `0.1.0`。
