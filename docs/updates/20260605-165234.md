# Implement config and logging systems

- 时间：20260605-165234
- 分支：main-update-config-20260605-165234
- 基准分支：main-update-20260605-164501

## 主要变更

- 实现 `config.json` 加载、缺失时从 `config.example.json` 自动创建、基础配置校验。
- 使用 Pydantic v2 定义基础配置模型。
- 实现控制台和文件双输出日志系统，日志文件写入 `modelpilot-gateway/logs/YYYYMMDD_HHMMSS.log`。
- 增加 API Key 脱敏工具，日志过滤器会屏蔽 `sk-` 格式密钥。
- 应用启动时加载配置并初始化日志。
- 增加配置加载、默认配置创建、密钥脱敏和健康检查测试。

## 验证方式

已执行：

```powershell
python -m pytest
```

结果：`6 passed, 1 warning`。
