# Add startup scripts and bilingual docs

- 时间：20260605-174622
- 分支：main-update-scripts-docs-20260605-174622
- 基准分支：main-update-validation-20260605-173948

## 主要变更

- 新增 Windows 一键启动脚本 `start_windows.bat`。
- 新增 Linux 一键启动脚本 `start_linux.sh`。
- 新增 `app/i18n.py`，根据 `server.language` 输出中文或英文启动提示。
- README 改为中英双语，补充启动方式、后端模型、Ollama、用户 API Key、权限、OpenAI Base URL、curl、Python SDK、FAQ、MVP 限制和路线图。
- 新增 `config.schema.md` 说明主要配置字段。

## 验证方式

已执行：

```powershell
python -m pytest
```

结果：`46 passed, 1 warning`。

已检查：

- `python -m py_compile app\i18n.py app\main.py`
- `start_windows.bat` 包含 Python 检查、venv、依赖安装和 uvicorn 启动命令
- `start_linux.sh` 包含 bash shebang、`set -e`、chmod 提示和 uvicorn 启动命令
