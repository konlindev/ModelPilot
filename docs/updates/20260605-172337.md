# Implement first-layer smart-auto rule routing

- 时间：20260605-172337
- 分支：main-update-smart-auto-20260605-172337
- 基准分支：main-update-auth-20260605-170414

## 主要变更

- 实现 `app/router_engine.py` 第一层规则路由。
- 支持 `model=smart-auto`，按关键词、任务类型、估算 token 和模型上下文窗口选择真实模型。
- 支持按用户 `allowed_models` fallback，跳过 disabled 模型。
- 响应中保留用户请求的 `model=smart-auto`，并在 `modelpilot` 中记录 `routed_model`、`backend_model`、`task_type`、`route_reason`。
- 默认配置新增真实模型 `tier` 和 `max_context_tokens`。
- 新增 `tests/test_router_engine.py`，覆盖 rewrite、listing、code、fallback、disabled、token 升级和 mock 后端转发。

## 验证方式

已执行：

```powershell
python -m pytest
```

结果：`30 passed, 1 warning`。

